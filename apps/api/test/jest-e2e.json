{
  "moduleFileExtensions": ["js", "json", "ts"],
  "rootDir": ".",
  "testEnvironment": "node",
  "testRegex": ".e2e-spec.ts$",
  "transform": {
    "^.+\\.(t|j)s$": "ts-jest"
  },
  "_setup_note": "Before running e2e tests, ensure DATABASE_URL is set and run: cd apps/api && npx prisma migrate deploy. A globalSetup script (test/global-setup.ts) will be added in a future slice to automate this."
}
